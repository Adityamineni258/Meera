import 'package:flutter/material.dart';

class CopingScreen extends StatelessWidget {
  const CopingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coping Screen'),
    );
  }
}