import 'package:flutter/material.dart';

class HelplinesScreen extends StatelessWidget {
  const HelplinesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Helplines Screen'),
    );
  }
}